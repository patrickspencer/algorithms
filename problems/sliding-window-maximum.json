{
 "id": "79",
 "slug": "sliding-window-maximum",
 "title": "Sliding Window Maximum",
 "tags": [
  "Array",
  "Queue",
  "Sliding Window",
  "Heap (Priority Queue)",
  "Monotonic Queue",
  "Range Minimum/Maximum Query"
 ],
 "statement": "You are monitoring a sequence of sensor readings collected over time, stored as an integer array. A window of fixed length slides across this array from left to right, one position at a time, starting at the very beginning of the array and stopping once it reaches the end.\n\nAt each position the window occupies, you must record the largest value contained within that window.\n\nYour task is to produce, in order, the list of these maximum values, one for every position the window visits as it slides from the start of the array to the end.\n\nFor example, if the window can hold `k` consecutive elements, the first value you record is the maximum among the first `k` elements, the second is the maximum among elements 2 through k+1, and so on, until the window has covered the final `k` elements of the array.\n\nDesign a solution that avoids recomputing the maximum from scratch at every step, since doing so would be too slow for large inputs.",
 "functionName": "maxSlidingWindow",
 "params": [
  {
   "name": "readings",
   "type": "number[]"
  },
  {
   "name": "windowSize",
   "type": "number"
  }
 ],
 "returns": "number[]",
 "examples": [
  {
   "input": "readings = [4, 10, 5, 2, 9, 1, 8], windowSize = 3",
   "output": "[10, 10, 9, 9, 9]",
   "explanation": "Window [4,10,5] -> max 10. Window [10,5,2] -> max 10. Window [5,2,9] -> max 9. Window [2,9,1] -> max 9. Window [9,1,8] -> max 9."
  },
  {
   "input": "readings = [7, 7, 7, 7], windowSize = 2",
   "output": "[7, 7, 7]",
   "explanation": "Every window of size 2 within this constant array contains only the value 7, so each recorded maximum is 7."
  }
 ],
 "constraints": [
  "1 <= readings.length <= 10^5",
  "-10^4 <= readings[i] <= 10^4",
  "1 <= windowSize <= readings.length"
 ]
}