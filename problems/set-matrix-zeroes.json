{
 "id": "75",
 "slug": "set-matrix-zeroes",
 "title": "Zero Out the Grid",
 "tags": [
  "Array",
  "Hash Table",
  "Matrix"
 ],
 "statement": "You are given a rectangular grid of integers, represented as a 2D array. Whenever a cell in the grid holds the value 0, every other cell that shares its row or shares its column must be overwritten with 0 as well.\n\nYour job is to apply this rule to the entire grid and produce the final result, modifying the grid directly (in place) rather than returning a brand-new structure.\n\nBecause zeroing a cell can itself trigger further zeroing of its row and column, you cannot simply scan the grid and zero cells as you find zeros, doing so naively would cause a cascade where newly-created zeros incorrectly wipe out additional rows and columns that shouldn't be touched. You must first determine, based only on the *original* contents of the grid, which rows and which columns need to be cleared, and then apply those clearings in a second pass.\n\nAim for a solution that avoids allocating a full second copy of the grid for auxiliary bookkeeping; using a small constant amount of extra space beyond the input (for example, by reusing part of the grid itself as marker flags) is the preferred approach, though a solution using an amount of extra memory proportional to the number of rows plus columns is also acceptable.",
 "functionName": "zeroOutGrid",
 "params": [
  {
   "name": "grid",
   "type": "number[][]"
  }
 ],
 "returns": "void (modify grid in place)",
 "examples": [
  {
   "input": "grid = [[1,2,3],[4,0,6],[7,8,9]]",
   "output": "[[1,0,3],[0,0,0],[7,0,9]]",
   "explanation": "The single 0 sits at row 1, column 1. So all of row 1 becomes zero, and all of column 1 becomes zero, while every other cell keeps its original value."
  },
  {
   "input": "grid = [[5,1,0,4],[3,6,2,7],[0,8,9,1]]",
   "output": "[[0,0,0,0],[0,6,0,7],[0,0,0,0]]",
   "explanation": "There are zeros at (row 0, col 2) and (row 2, col 0). Row 0, row 2, column 0, and column 2 must all be cleared. Cell (1,1)=6 and cell (1,3)=7 lie outside every affected row/column, so they remain unchanged."
  }
 ],
 "constraints": [
  "1 <= number of rows in grid, number of columns in grid <= 300",
  "-2^31 <= grid[i][j] <= 2^31 - 1",
  "The grid contains at least one row and one column"
 ]
}