{
 "id": "73",
 "slug": "rotate-image",
 "title": "Rotate the Square Grid In Place",
 "tags": [
  "Array",
  "Math",
  "Matrix"
 ],
 "statement": "You are given a square grid of integers, represented as an n x n matrix. Your task is to rotate the grid by 90 degrees clockwise, transforming it directly within the same matrix structure rather than building and returning a brand-new grid.\n\nThink of the grid as a photograph made of pixels arranged in rows and columns: after the rotation, whatever pixel value sat in the top-left corner should now sit in the top-right corner, and the whole image should appear turned a quarter-turn clockwise, with all edges and corners repositioned accordingly.\n\nBecause the transformation must happen in place, you are not allowed to allocate a second n x n matrix to hold the answer and then copy it back, the rearrangement of values must occur using only a small, constant amount of extra scratch space (such as a handful of temporary variables), regardless of how large n is.",
 "functionName": "rotateGridClockwise",
 "params": [
  {
   "name": "grid",
   "type": "number[][]"
  }
 ],
 "returns": "void",
 "examples": [
  {
   "input": "grid = [[1,2],[3,4]]",
   "output": "[[3,1],[4,2]]",
   "explanation": "A 2x2 grid rotated 90 degrees clockwise moves the bottom-left value (3) to the top-left, the top-left value (1) to the top-right, the bottom-right value (4) stays adjacent below the new top-right value, and the top-right value (2) moves to the bottom-right."
  },
  {
   "input": "grid = [[5,1,9],[2,7,3],[8,4,6]]",
   "output": "[[8,2,5],[4,7,1],[6,3,9]]",
   "explanation": "Each column of the original grid, read from bottom to top, becomes a row of the rotated grid: the first column (5,2,8) read bottom-to-top gives the new first row (8,2,5), the second column (1,7,4) read bottom-to-top gives the new second row (4,7,1), and the third column (9,3,6) read bottom-to-top gives the new third row (6,3,9)."
  }
 ],
 "constraints": [
  "grid.length == grid[i].length == n",
  "1 <= n <= 20",
  "-1000 <= grid[i][j] <= 1000",
  "The matrix must be modified in place using O(1) extra space"
 ]
}